export enum RoomMode {
    INDIVIDUAL = 'INDIVIDUAL',
    TEAM = 'TEAM',
}

export enum EventType {
    HOST = 'HOST',
    SPOT_DEVICE = 'SPOT_DEVICE',
}

export enum SocketDataType {
    EVENT_HOST = 'EVENT_HOST',
    EVENT_USER = 'EVENT_USER',
    NORMAL_USER = 'NORMAL_USER',
}